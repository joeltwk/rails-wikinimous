<div class="container">
<h1><%= @article.title %></h1>
<%= link_to "Delete", article_path, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
<p><%= @article.content %></p>

<%= link_to "Edit", edit_article_path(params[:id]), :class => 'btn btn-primary' %>
<%= link_to "Back", articles_path, :class => 'btn btn-primary' %>
</div>
